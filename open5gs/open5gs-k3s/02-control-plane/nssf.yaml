# Service
---
apiVersion: v1
kind: Service
metadata:
  name: nssf
  namespace: open5gs
spec:
  selector:
    app: nssf
  ports:
  - port: 7777
    targetPort: 7777

# StatefulSet
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: nssf
  namespace: open5gs
spec:
  serviceName: nssf
  replicas: 1
  selector:
    matchLabels:
      app: nssf
  template:
    metadata:
      labels:
        app: nssf
        component: control-plane
    spec:
      initContainers:
      - name: wait-for-dependency
        image: busybox:1.35
        command:
        - sh
        - -c
        - |
          until nc -z nrf.open5gs.svc.cluster.local 7777; do
            echo "Waiting for nrf..."
            sleep 2
          done
      
      containers:
      - name: nssf
        image: open5gs-nssf:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 7777
        env:
        - name: DB_URI
          value: "mongodb://mongodb.open5gs.svc.cluster.local:27017/open5gs"
        volumeMounts:
        - name: config
          mountPath: /etc/open5gs/custom
        - name: logs
          mountPath: /var/log/open5gs
          subPath: nssf
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          exec:
            command: ["pgrep", "open5gs-nssfd"]
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          exec:
            command: ["pgrep", "open5gs-nssfd"]
          initialDelaySeconds: 5
          periodSeconds: 10
      
      volumes:
      - name: config
        configMap:
          name: nssf-config
      - name: logs
        hostPath:
          path: /mnt/data/open5gs-logs
          type: DirectoryOrCreate